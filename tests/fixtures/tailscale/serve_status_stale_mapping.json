{
  "_comment": "Synthetic fixture, documented shape (KI-17 layer 2). Not captured from a real machine: this repo's `tailscale serve status --json` currently returns `{}` (nothing configured). Hostname is a synthetic placeholder, not a real tailnet name. Mimics an operator having hand-configured `tailscale serve` against the pre-6b Studio port (8787) months ago and forgotten about it -- exactly the KI-17 scenario preflight must refuse.",
  "TCP": {
    "443": {
      "HTTPS": true
    }
  },
  "Web": {
    "example-host.example-tailnet.ts.net:443": {
      "Handlers": {
        "/": {
          "Proxy": "http://127.0.0.1:8787"
        }
      }
    }
  },
  "AllowFunnel": {}
}
